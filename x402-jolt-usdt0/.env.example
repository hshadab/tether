# =============================================================================
# x402-jolt-usdt0 Configuration
# =============================================================================
# Copy this file to .env and fill in required values.
# Run: cp .env.example .env

# -----------------------------------------------------------------------------
# REQUIRED — These must be set before running
# -----------------------------------------------------------------------------

# Client wallet seed phrase (BIP-39 mnemonic)
# This wallet signs and pays for weather API requests.
# Derive its address: node -e "import('ethers').then(e => console.log(e.Wallet.fromPhrase('<mnemonic>').address))"
# Fund this wallet with USDT0 + XPL gas on Plasma (1 USDT0 = 10,000 requests)
# WARNING: Do NOT use the Hardhat default mnemonic ("test test ... junk") —
# USDT0 on Plasma blacklists its well-known addresses.
# Generate a fresh one: node -e "import('ethers').then(e => console.log(e.Wallet.createRandom().mnemonic.phrase))"
MNEMONIC=

# Server wallet address that receives payments
# Generate a new one: node -e "import('ethers').then(e => { const w = e.Wallet.createRandom(); console.log('Address:', w.address, '\nKey:', w.privateKey) })"
PAY_TO_ADDRESS=

# Cosigner private key (hex, no 0x prefix)
# Generate: openssl rand -hex 32
# WARNING: Never commit real private keys!
COSIGNER_PRIVATE_KEY=

# -----------------------------------------------------------------------------
# OPTIONAL — Defaults work for local development
# -----------------------------------------------------------------------------

# Cosigner service URL (default: http://localhost:3001)
# Change if running cosigner on a different host/port
COSIGNER_URL=http://localhost:3001

# Prover binary path (default: ../prover/target/release/zkml-prover)
# Only change if you moved the prover binary
PROVER_BINARY=

# Models directory containing authorization.onnx + vocab.json (default: ../models)
MODELS_DIR=

# Server port (default: 4020)
SERVER_PORT=4020

# Facilitator port (default: 4021)
FACILITATOR_PORT=4021

# -----------------------------------------------------------------------------
# NETWORK — Plasma (default) or Sepolia testnet
# -----------------------------------------------------------------------------

# Set to "true" to use Sepolia testnet instead of Plasma mainnet
# Sepolia is free (use faucet tokens) — good for development
USE_SEPOLIA=

# Sepolia RPC URL (only used if USE_SEPOLIA=true)
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_KEY

# Sepolia USDT contract (only used if USE_SEPOLIA=true)
SEPOLIA_USDT_ADDRESS=0x959413cfD31eBe4Bc81A57b284cD638b4Be88500

# Plasma RPC URL (default: https://rpc.plasma.to)
# Only change if using a custom RPC endpoint
PLASMA_RPC_URL=
